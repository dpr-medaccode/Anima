{
  "name": "anima-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wasm-pack build ../../canvas/Anima-canvas --target web && wasm-pack build ../../engine/Anima-engine --target web && vite",
    "build::web": "vite build",
    "build:wasm": "wasm-pack build ../../canvas/Anima-canvas --target web && wasm-pack build ../../engine/Anima-engine --target web",
    "build": "wasm-pack build ../../canvas/Anima-canvas --target web && wasm-pack build ../../engine/Anima-engine --target web && vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.12.2",
    "svelte": "^5.55.4",
    "svelte-check": "^4.4.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  }
}